# Weave — Agent Guide

## 项目概述

基于节点的图编辑器，支持自由摆放节点并通过 socket 建立连线关系，可用于创建思维导图、流程图、逻辑树、泳道图等多种图类型。

## 技术栈

纯 JavaScript + HTML5 + CSS3 + Canvas 2D + SVG + localStorage。单 HTML 文件，无框架、无构建步骤，运行时唯一的外部请求是 CDN 上的字体。

## 核心约束

### 1. 始终保持为单 HTML 文件

`APPs/Weave.html` 是 Weave 唯一的应用程序文件，严禁拆分为多文件结构。所有 HTML、CSS、JavaScript 必须内联在同一文件中。外部资源（如字体）从 CDN 加载，不引入本地文件依赖。

同目录下 `APPs/Weave.min.html` 是 `test/build-min.js` 生成的压缩发布版，不手工编辑；同目录的其他 HTML 文件为独立的历史应用，与 Weave 无关。

### 2. 每次修改后必须测试

先运行自动化测试，命令与分组见 `test/README.md`：

- `cd test && npm run test:fast` — 版本号校验、纯函数单测与结构守护，秒级
- `cd test && npm run test:gui` — GUI 冒烟，分钟级

自动化测试无法覆盖的交互（真实鼠标手势、视觉呈现等），必须要求人类手动测试，并清晰列出本次涉及变化的功能范围，让人类知道需要重点测试什么。

### 3. 代码分区规范（模块化拼接）

`APPs/Weave.html` 的 `<script>` 采用"模块化拼接"形态：13 个模块按固定顺序排列
（M01 常量与工具 → M13 装配与启动），每个模块以 `/* ══ Mxx · 名称 ──` 开头、其后两行为
`职责:` 与 `依赖:` 的块注释为界，头部文档含模块清单与数据模型说明。修改代码时遵守：

- 新增方法 = 加入对应模块的 `Object.assign(App, {...})` 块（简写方法，禁止箭头函数）
- 纯函数优先放入 `Weave.Util/Color/Geom` 命名空间（M01），不要在 App 上堆工具
- 结构变更后运行 `cd test && npm run test:structure`（结构守护：banner 顺序 + 条目逐条等价）
- 方法体修改会使条目比对失败，该失败代表基线过期而非功能回归：运行 `node test/check-structure.js --snapshot` 重新生成基线，并核对差异只包含本次预期的方法修改
- `node test/diff-test.js` 对比 git HEAD 原版与工作区，仅模块化重构前适用，不纳入 `npm test` 与 CI

## 文档分配

项目说明文档统一放在 docs/ 目录，各文件分工如下：

- **development-workflow.md**：开发流程。功能修改按 实现功能 → 测试功能 → 优化代码质量 三阶段顺序执行，并规定各阶段的具体做法、测试命令与提交信息规范。
- **documentation-and-comments.md**：文档与注释规范。规定 docs/ 文档与代码注释的书写规则（措辞、结构与表达约束），文件开头给出适用范围。
- **application-form.md**：软件本体形式。描述 APPs/Weave.html 的存在形式、技术栈、界面语言、数据模型、渲染结构、模块划分与代码规则。
- **weave_docs_guide.json**：文档导读图。以本软件画布格式存储的 docs/ 导读节点图，可在画布中导入查看。

docs/ 之外的相关文档：

- **test/README.md**：测试台架、测试清单与结构守护的说明。
- **UnsupportedBrowsers/README.md**：不兼容浏览器的记录与处理结论。
- **CHANGELOG.md**：按版本记录新功能、改进与修复。

发布流程说明只在私有开发库维护（docs/release-workflow.md），描述两库的对应关系、同步时排除的路径与版本发布步骤；公开发布库不含该文件。

引用关系：development-workflow.md 引用本文件作为代码结构规则的全文来源；application-form.md 引用 test/README.md 与 development-workflow.md 作为结构与流程的延伸说明。本文档为开发规则的总入口。

## 功能概览

- **节点**：添加、删除、选中（单击 / 框选 / 多选）、拖拽移动、内联编辑、复制粘贴、调整节点大小
- **连线**：socket 拖拽创建、删除、标签编辑、贝塞尔曲线自定义调整
- **画框分区**：Region 键（默认 Shift+R）/ Alt+左键拖拽创建分区；整体移动（联动框内节点）、调整大小；右键属性 / 移动分区框 / 调整分区框大小 / 删除；节点拖入自动归属；分区可嵌套
- **节点串收起**：点击节点右侧输出连接点的 − 徽标收起其后相连的节点串，+ 徽标展开；支持嵌套
- **关联高亮**：按住 Alt 单击节点，高亮直接关联的节点与连线，其余内容加轻微模糊
- **画布**：平移（拖拽 / 中键）、滚轮缩放、适应视图、坐标 HUD 编辑
- **颜色**：5 种预设色 + 自定义色轮选择器，Ctrl+滚轮快速切换
- **对齐设置**：节点位置、节点大小、分区位置、分区大小共 4 项，默认全部开启，可一键重置
- **导入/导出**：JSON 导入导出（含视口信息）、PNG 导出、拖拽 JSON 文件导入
- **其他**：撤销/重做（最多 50 步）、只读模式、专注模式（Ctrl+H）、键位设置（首次启动自动弹出，快捷键可自定义）、语言切换（设置内：中文 / English）、自动保存到 localStorage
- **Σ 彩蛋**：单击顶栏品牌区触发 `App.spinAboutSigma`，向画布抛出一个节点
