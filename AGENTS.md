# Weave — Agent Guide

## 项目概述

基于节点的图编辑器，支持自由摆放节点并通过 socket 建立连线关系，可用于创建思维导图、流程图、逻辑树、泳道图等多种图类型。

## 技术栈

纯 JavaScript + HTML5 + CSS3 + Canvas 2D + SVG + localStorage。单 HTML 文件，零外部依赖。

## 核心约束

### 1. 始终保持为单 HTML 文件

`APPs/Weave.html` 是唯一的应用程序文件，严禁拆分为多文件结构。所有 HTML、CSS、JavaScript 必须内联在同一文件中。外部资源（如字体）应从 CDN 加载，不得引入本地文件依赖。

### 2. 每次修改后必须进行冒烟测试

如果 AI 无法自动完成测试（如涉及 GUI 交互），必须要求人类手动测试，并清晰列出本次涉及变化的功能范围，让人类知道需要重点测试什么。

### 3. 代码分区规范（模块化拼接）

`APPs/Weave.html` 的 `<script>` 采用"模块化拼接"形态：13 个模块按固定顺序排列
（M01 常量与工具 → M13 装配与启动），每个模块以 `/* ══ M01 · 名称 ══ */` 锚点注释为界，
头部文档含模块清单与数据模型约定。修改代码时遵守：

- 新增方法 = 加入对应模块的 `Object.assign(App, {...})` 块（简写方法，禁止箭头函数）
- 纯函数优先放入 `Weave.Util/Color/Geom` 命名空间（M01），不要在 App 上堆工具
- 结构变更后运行 `node test/check-structure.js`（结构守护：banner 顺序 + 条目逐条等价）
- 每次修改后运行 `node test/gui-smoke.js`（冒烟）
- 大规模重构后运行 `node test/diff-test.js`（与原版逐步骤一致性比对）

## 文档分配（docs/）

项目说明文档统一放在 docs/ 目录，各文件分工如下：

- **development-workflow.md**：开发流程。功能修改按 实现功能 → 测试功能 → 优化代码质量 三阶段顺序执行，并规定各阶段的具体做法。
- **documentation-and-comments.md**：文档与注释规范。编写或修改任何文档、注释时必须遵守的书写规则（措辞、结构与表达约束）。
- **application-form.md**：软件本体形式。描述 APPs/Weave.html 的存在形式（单 HTML 文件、内联样式脚本）、技术栈、界面语言与浏览器兼容性说明的位置。
- **weave_docs_guide.json**：文档导读图。以本软件画布格式存储的 docs/ 导读节点图，可在画布中导入查看。

以上文档中，development-workflow.md 与 application-form.md 引用本文件（AGENTS.md）作为代码结构规则的全文来源；本文档为开发约定的总入口。

## 功能概览

- **节点**：添加、删除、选中（单击 / 框选 / 多选）、拖拽移动、内联编辑、复制粘贴
- **连线**：socket 拖拽创建、删除、标签编辑、贝塞尔曲线自定义调整
- **画框分区**：Region 键（默认 Shift+R）/ Alt+左键拖拽创建分区；整体移动（联动框内节点）；右键属性 / 移动分区框 / 调整分区框大小 / 删除；节点拖入自动归属
- **画布**：平移、滚轮缩放、适应视图、坐标 HUD 编辑
- **颜色**：5 种预设色 + 自定义色轮选择器
- **导入/导出**：JSON 导入导出（含视口信息）、PNG 导出、拖拽 JSON 文件导入
- **其他**：撤销/重做（最多 50 步）、只读模式、自动保存到 localStorage