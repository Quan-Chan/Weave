# 更新记录

## v1.3

### 新功能

- 关联高亮：按住 Alt 单击节点，高亮该节点与其直接关联的节点、连线；被抬起的节点保持清晰，其余节点、连线、分区与网格加轻微模糊；按住 Alt 点击其他节点可切换高亮对象
- 对齐设置：4 项对齐默认全部开启，常规页新增「重置对齐设置」一键恢复

### 修复

- 修复关联高亮时连线被节点遮住：高亮连线与高亮节点一并置顶，连线不再被未高亮的节点挡住
- 修复清除高亮后连线元素归位串组，导致再次高亮时连线仍停留在模糊层
- 修复分区创建预览框颜色不跟随顶栏生成色
- 修复输出连接点收起徽标在拖拽建线期间消失
- 修复删除最后一条连线后收起徽标残留
- 修复 PNG 导出中可收起连接点中心元素缺失（快照路径与旧版重绘路径）

### 改进

- 节点层拆分为主层与高亮层，关联高亮的模糊对整层一次生效
- 关联高亮、键位与对齐设置测试纳入 npm test 与 CI

## v1.2.1

### 修复

- 修复新建/克隆节点落入分区后不加入分区成员表的问题：双击分区内新建、右键复制、粘贴的节点，移动分区时不再被落下，随分区一同移动

## v1.2

### 新功能

- 画框分区：Region 键或 Alt+拖拽框出区域；支持整体移动、调整大小、编辑名称与颜色；节点拖入自动归属，分区可嵌套
- 节点串收起/展开：点击节点右侧输出点徽标，收起或展开其后相连的节点串；支持嵌套收起

### 改进

- 存档尺寸单位统一为格（1 格 = 20px），与位置单位一致
- 弹窗颜色选择样式修复
- 新增压缩版 APPs/Weave.min.html（体积约为完整版 43%，功能一致）

## v1.1.1

### 修复

- 修复 Firefox 中连线异常粗与缩放抖动（连线描边宽度改为属性直写）
- 修复 Firefox 下 PNG 快照导出失效（瓦片 SVG 样式 XML 转义，导出管线恢复）
- 兼容导出通道的箭头与屏幕渲染同形

## v1.1

### 新功能

- 调整节点大小：右键菜单选择「调整节点大小」，拖拽四角手柄自由调整节点宽高；可一键重置
- 专注模式（Ctrl+H）：隐藏顶部工具栏与侧边栏，画布全屏
- 语言切换：设置弹窗内切换中文 / English
- 键位设置：快速入门弹窗中可自定义快捷键
- 对齐设置：节点位置、节点大小可对齐网格（设置弹窗内开关）

### 其他

- 附 250 节点性能测试存档

## v1.0

### 新功能

- 节点：添加、删除、选中、拖拽移动、内联编辑、复制粘贴
- 连线：socket 拖拽创建、删除、标签编辑、贝塞尔曲线自定义调整
- 画布：平移、滚轮缩放、适应视图、坐标 HUD
- 颜色：5 种预设色 + 自定义色轮选择器
- 导入/导出：JSON 导入导出、PNG 导出、拖拽文件导入
- 只读模式、自动保存到 localStorage
